# 基于Spring Boot的力量训练负荷与恢复监控系统

## 项目概述

本系统是一个专注于力量训练的负荷计算与恢复监控平台，旨在帮助健身爱好者科学管理训练强度、评估身体恢复状态，并提供个性化训练建议。系统采用Spring Boot 3.2.5作为后端框架，Vue 3作为前端框架，实现了完整的训练数据管理、负荷计算算法、恢复状态评估和数据可视化功能。

## 核心功能范围

### 1. 用户管理
- 用户注册与登录
- 个人信息管理（身高、体重、年龄、性别等）
- 训练经验设置
- 密码修改

### 2. 训练数据录入
- 手动录入训练数据（动作名称、重量、组数、次数等）
- 训练记录历史查询
- 力量训练和有氧训练分类管理

### 3. 负荷计算算法
- 自动计算训练量（重量×组数×次数）
- 1RM（最大重量）估算
- 训练负荷趋势分析
- 负荷数据可视化展示

### 4. 恢复状态评估
- 基于睡眠时长和压力水平的恢复评分
- 恢复状态分类（恢复良好、一般、轻度疲劳、中度疲劳、严重疲劳）
- 个性化恢复建议

### 5. 数据可视化展示
- 训练负荷趋势图表
- 恢复状态仪表盘
- 身体数据统计（BMI、基础代谢等）
- 训练记录数据表格

### 6. 训练建议生成
- 基于恢复状态的训练强度建议
- 训练动作平衡分析
- 周训练计划建议
- 训练小贴士

### 7. 营养追踪
- 每日营养摄入记录
- 卡路里计算
- 营养目标设置

### 8. 管理端功能
- 系统监控仪表盘
- 用户管理
- 数据导出
- 审计日志

## 技术栈

### 后端技术
| 技术 | 版本 | 说明 |
|------|------|------|
| Spring Boot | 3.2.5 | 核心框架 |
| Spring Data JPA | 3.2.5 | ORM框架 |
| Spring Security | 6.2.x | 安全框架 |
| MySQL | 8.0.33 | 生产数据库 |
| H2 Database | - | 测试数据库 |
| JWT (jjwt) | 0.11.5 | 令牌认证 |
| Caffeine | - | 本地缓存 |
| Redis | - | 分布式缓存（可选） |
| Apache POI | 5.2.5 | Excel导出 |
| SpringDoc OpenAPI | 2.2.0 | API文档 |
| Lombok | 1.18.34 | 代码简化 |
| Logstash Encoder | 7.4 | JSON日志 |
| Micrometer | - | 监控指标 |
| Gatling | 3.10.3 | 性能测试 |
| jqwik | 1.8.2 | 属性测试 |

### 前端技术
| 技术 | 版本 | 说明 |
|------|------|------|
| Vue | 3.3.4 | 核心框架 |
| Vue Router | 4.2.4 | 路由管理 |
| Pinia | 2.1.6 | 状态管理 |
| Element Plus | 2.8.0 | UI组件库 |
| Vant | 4.9.22 | 移动端组件 |
| ECharts | 5.4.3 | 图表库 |
| Axios | 1.5.0 | HTTP客户端 |
| Vite | 4.4.9 | 构建工具 |
| Vitest | 4.0.16 | 单元测试 |
| Playwright | 1.57.0 | E2E测试 |
| Sass | 1.93.3 | CSS预处理 |
| Sentry | 10.32.1 | 错误监控 |

### 构建工具
| 工具 | 版本 | 说明 |
|------|------|------|
| Maven | 3.6+ | 后端构建 |
| Node.js | 16+ | 前端运行时 |
| npm | - | 包管理 |
| Docker | - | 容器化部署 |

## 项目结构

```
Fitness/
├── admin/                          # 管理端前端 (Vue 3)
│   ├── src/
│   │   ├── assets/styles/          # 样式文件
│   │   ├── components/             # 组件
│   │   │   ├── DataExport.vue      # 数据导出组件
│   │   │   ├── SystemMonitor.vue   # 系统监控组件
│   │   │   └── UserActivityChart.vue # 用户活跃度图表
│   │   ├── router/                 # 路由配置
│   │   ├── stores/                 # Pinia状态管理
│   │   └── views/                  # 页面视图
│   ├── package.json
│   └── vite.config.js
│
├── frontend/                       # 用户端前端 (Vue 3)
│   ├── e2e/                        # E2E测试 (Playwright)
│   ├── src/
│   │   ├── api/                    # API接口封装
│   │   ├── assets/styles/          # 样式文件
│   │   ├── components/             # 通用组件
│   │   ├── composables/            # 组合式函数
│   │   ├── directives/             # 自定义指令
│   │   ├── modules/                # 业务模块 (模块化架构)
│   │   │   ├── auth/               # 认证模块
│   │   │   ├── dashboard/          # 仪表盘模块
│   │   │   ├── training/           # 训练模块
│   │   │   ├── nutrition/          # 营养模块
│   │   │   └── settings/           # 设置模块
│   │   ├── router/                 # 路由配置
│   │   │   ├── modules/            # 模块路由
│   │   │   └── index.js            # 主路由
│   │   ├── stores/                 # Pinia状态管理
│   │   ├── utils/                  # 工具函数
│   │   └── views/                  # 页面视图
│   ├── tests/                      # 测试配置
│   ├── package.json
│   └── vite.config.js
│
├── shared/                         # 前端共享代码 (共享内核)
│   ├── api/                        # 共享API封装
│   ├── components/                 # 共享组件
│   ├── composables/                # 共享组合函数
│   ├── layouts/                    # 布局组件
│   ├── plugins/                    # 插件
│   ├── styles/                     # 共享样式
│   └── utils/                      # 共享工具函数
│
├── src/main/java/com/wzl/fitness/  # 后端源码
│   ├── annotation/                 # 自定义注解
│   ├── aspect/                     # AOP切面
│   ├── common/                     # 通用组件
│   ├── config/                     # 配置类
│   ├── controller/                 # 控制器层
│   ├── dto/                        # 数据传输对象
│   ├── entity/                     # 实体类
│   ├── exception/                  # 异常处理
│   ├── filter/                     # 过滤器
│   ├── health/                     # 健康检查
│   ├── interceptor/                # 拦截器
│   ├── model/                      # 模型类
│   ├── modules/                    # 业务模块 (模块化架构)
│   │   ├── user/                   # 用户模块
│   │   ├── training/               # 训练模块
│   │   ├── nutrition/              # 营养模块
│   │   ├── recovery/               # 恢复评估模块
│   │   └── admin/                  # 管理模块
│   ├── repository/                 # 数据访问层
│   ├── security/                   # 安全组件
│   ├── service/                    # 业务逻辑层
│   ├── shared/                     # 共享内核 (模块化架构)
│   │   ├── config/                 # 共享配置
│   │   └── event/                  # 事件基础设施
│   ├── util/                       # 工具类
│   └── vo/                         # 视图对象
│
├── src/main/resources/             # 资源文件
├── src/test/                       # 测试代码
├── docker/                         # Docker配置
├── docs/                           # 文档
├── scripts/                        # 脚本
├── logs/                           # 日志目录
│
├── docker-compose.yml              # Docker编排
├── pom.xml                         # Maven配置
└── README.md                       # 项目说明
```

## 模块化架构

系统采用领域驱动设计(DDD)原则，将单体应用重构为高内聚、低耦合的模块化架构。

### 架构概览

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           Application Layer                              │
├─────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────────────┐│
│  │ User Module │ │Training Mod │ │Nutrition Mod│ │  Recovery Module    ││
│  │             │ │             │ │             │ │                     ││
│  │ - Auth      │ │ - Records   │ │ - Records   │ │ - Assessment        ││
│  │ - Profile   │ │ - Plans     │ │ - Goals     │ │ - Suggestions       ││
│  │ - Settings  │ │ - Stats     │ │ - Stats     │ │ - Trends            ││
│  └──────┬──────┘ └──────┬──────┘ └──────┬──────┘ └──────────┬──────────┘│
│         │               │               │                    │           │
│  ┌──────┴───────────────┴───────────────┴────────────────────┴──────────┐│
│  │                         Event Bus (Spring Events)                     ││
│  └──────────────────────────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────────────────────────┤
│                           Shared Kernel                                  │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────────────────┐│
│  │  Security  │ │   Common   │ │   Config   │ │     Infrastructure     ││
│  │  - JWT     │ │  - Response│ │  - Cache   │ │  - Exception Handler   ││
│  │  - Auth    │ │  - Page    │ │  - DB      │ │  - Logging             ││
│  └────────────┘ └────────────┘ └────────────┘ └────────────────────────┘│
└─────────────────────────────────────────────────────────────────────────┘
```

### 模块说明

| 模块 | 路由前缀 | 说明 | 依赖 |
|------|----------|------|------|
| 用户模块 (user) | `/api/v1/user`, `/api/v1/auth` | 用户认证、用户管理、用户资料 | - |
| 训练模块 (training) | `/api/v1/training` | 训练记录、训练计划、训练分析 | user |
| 营养模块 (nutrition) | `/api/v1/nutrition` | 营养记录、营养目标、营养建议 | user |
| 恢复评估模块 (recovery) | `/api/v1/recovery` | 恢复状态评估、训练建议 | user, training |
| 管理模块 (admin) | `/api/v1/admin` | 系统监控、数据导出、缓存管理 | user |

### 模块配置

各模块可通过 `application.properties` 配置启用/禁用：

```properties
# 用户模块配置
fitness.modules.user.enabled=true
fitness.modules.user.order=1
fitness.modules.user.description=用户认证和管理模块

# 训练模块配置
fitness.modules.training.enabled=true
fitness.modules.training.order=2
fitness.modules.training.dependencies=user
fitness.modules.training.description=训练记录和计划管理模块

# 营养模块配置
fitness.modules.nutrition.enabled=true
fitness.modules.nutrition.order=3
fitness.modules.nutrition.dependencies=user
fitness.modules.nutrition.description=营养摄入记录和目标管理模块

# 恢复评估模块配置
fitness.modules.recovery.enabled=true
fitness.modules.recovery.order=4
fitness.modules.recovery.dependencies=user,training
fitness.modules.recovery.description=恢复状态评估和建议模块

# 管理模块配置
fitness.modules.admin.enabled=true
fitness.modules.admin.order=5
fitness.modules.admin.dependencies=user
fitness.modules.admin.description=系统管理和监控模块
```

### 模块接口

每个模块通过定义的接口对外提供服务，实现模块间松耦合：

```java
// 用户模块接口
public interface UserModuleApi {
    UserDTO getUserById(Long userId);
    UserDTO getUserByUsername(String username);
    boolean existsById(Long userId);
    UserBasicInfo getBasicInfo(Long userId);
}

// 训练模块接口
public interface TrainingModuleApi {
    List<TrainingRecordDTO> getRecentRecords(Long userId, int limit);
    TrainingLoadDTO calculateLoad(Long userId, LocalDate startDate, LocalDate endDate);
    TrainingStatsDTO getStats(Long userId);
}
```

### 事件驱动通信

模块间通过事件总线进行异步通信：

```java
// 训练完成事件
public class TrainingCompletedEvent extends DomainEvent {
    private final Long trainingRecordId;
    private final Double totalVolume;
    private final String exerciseType;
}

// 恢复模块监听训练完成事件
@EventListener
public void handleTrainingCompleted(TrainingCompletedEvent event) {
    // 自动更新恢复状态评估
}
```

### 获取模块配置

前端可通过API获取当前启用的模块列表：

```javascript
// GET /api/v1/modules/enabled
{
  "code": 200,
  "data": {
    "user": true,
    "training": true,
    "nutrition": true,
    "recovery": true,
    "admin": true
  }
}
```


## 开发环境搭建

### 环境要求

| 软件 | 版本要求 | 说明 |
|------|----------|------|
| JDK | 17+ | 后端运行时 |
| MySQL | 8.0+ | 生产数据库 |
| Node.js | 16+ | 前端运行时 |
| Maven | 3.6+ | 后端构建 |
| npm | 8+ | 前端包管理 |

### 1. 安装 JDK 17

**Windows:**
1. 下载 [Oracle JDK 17](https://www.oracle.com/java/technologies/downloads/#java17) 或 [OpenJDK 17](https://adoptium.net/)
2. 运行安装程序
3. 配置环境变量：
   - `JAVA_HOME`: JDK安装路径
   - `PATH`: 添加 `%JAVA_HOME%\bin`
4. 验证安装：`java -version`

**macOS:**
```bash
brew install openjdk@17
echo 'export PATH="/usr/local/opt/openjdk@17/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc
java -version
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install openjdk-17-jdk
java -version
```

### 2. 安装 MySQL 8.0

**Windows:**
1. 下载 [MySQL Installer](https://dev.mysql.com/downloads/installer/)
2. 运行安装程序，选择 "MySQL Server 8.0"
3. 配置root密码
4. 启动MySQL服务

**macOS:**
```bash
brew install mysql@8.0
brew services start mysql@8.0
mysql_secure_installation
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install mysql-server-8.0
sudo systemctl start mysql
sudo mysql_secure_installation
```

**创建数据库:**
```sql
CREATE DATABASE fitness_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'fitness'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON fitness_db.* TO 'fitness'@'localhost';
FLUSH PRIVILEGES;
```

### 3. 安装 Node.js

**Windows:**
1. 下载 [Node.js LTS](https://nodejs.org/)
2. 运行安装程序
3. 验证安装：`node -v` 和 `npm -v`

**macOS:**
```bash
brew install node@18
node -v
npm -v
```

**Linux (Ubuntu/Debian):**
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs
node -v
npm -v
```

### 4. 安装 Maven

**Windows:**
1. 下载 [Maven](https://maven.apache.org/download.cgi)
2. 解压到目标目录
3. 配置环境变量：
   - `MAVEN_HOME`: Maven安装路径
   - `PATH`: 添加 `%MAVEN_HOME%\bin`
4. 验证安装：`mvn -v`

**macOS:**
```bash
brew install maven
mvn -v
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt install maven
mvn -v
```

### 5. 配置项目

**后端配置:**

编辑 `src/main/resources/application.properties`:
```properties
# 数据库配置
spring.datasource.url=jdbc:mysql://localhost:3306/fitness_db?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8mb4
spring.datasource.username=fitness
spring.datasource.password=your_password

# JWT配置 (密钥至少512位)
jwt.secret=your_very_long_secret_key_at_least_512_bits_long_for_hs512_algorithm_security

# 服务端口
server.port=8080
```

**前端配置:**

前端默认连接 `http://localhost:8080` 后端API，如需修改，编辑 `frontend/vite.config.js` 中的代理配置。

### 6. 启动项目

**方式一：使用启动脚本 (推荐)**
```powershell
# Windows PowerShell
cd Fitness
.\start-all.ps1
```

**方式二：手动启动**

```bash
# 1. 启动后端
cd Fitness
mvn clean install -DskipTests
mvn spring-boot:run

# 2. 启动用户端前端 (新终端)
cd Fitness/frontend
npm install
npm run dev

# 3. 启动管理端前端 (新终端)
cd Fitness/admin
npm install
npm run dev
```

### 7. 访问应用

| 服务 | 地址 | 说明 |
|------|------|------|
| 后端API | http://localhost:8080 | Spring Boot服务 |
| 用户端 | http://localhost:3001 | Vue用户界面 |
| 管理端 | http://localhost:3002 | Vue管理界面 |
| Swagger UI | http://localhost:8080/swagger-ui.html | API文档 |
| Actuator | http://localhost:8080/actuator | 监控端点 |

## API 接口概览

### Swagger/OpenAPI 文档

系统集成了 SpringDoc OpenAPI，提供交互式API文档，支持模块化API分组：

- **Swagger UI**: http://localhost:8080/swagger-ui.html
- **OpenAPI JSON**: http://localhost:8080/api-docs
- **OpenAPI YAML**: http://localhost:8080/api-docs.yaml

### API 分组

Swagger UI 支持按模块查看API文档：

| 分组 | 说明 | 路由前缀 |
|------|------|----------|
| 全部API | 所有模块的API接口 | `/api/**` |
| 用户模块 | 用户认证、用户管理、用户资料 | `/api/v1/auth/**`, `/api/v1/user/**` |
| 训练模块 | 训练记录、训练计划、训练分析 | `/api/v1/training/**` |
| 营养模块 | 营养记录、营养目标、营养建议 | `/api/v1/nutrition/**` |
| 恢复评估模块 | 恢复状态评估、训练建议 | `/api/v1/recovery/**` |
| 管理模块 | 系统监控、数据导出、缓存管理 | `/api/v1/admin/**` |
| 仪表盘模块 | 数据概览、统计数据 | `/api/v1/dashboard/**` |
| 系统模块 | 健康检查、模块配置 | `/api/v1/modules/**`, `/actuator/**` |

### 主要API端点

#### 认证相关 (`/api/v1/auth`)
| 方法 | 端点 | 说明 |
|------|------|------|
| POST | `/register` | 用户注册 |
| POST | `/login` | 用户登录 |
| GET | `/me` | 获取当前用户信息 |
| POST | `/refresh` | 刷新令牌 |

#### 用户管理 (`/api/v1/users`)
| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/profile` | 获取用户资料 |
| PUT | `/profile` | 更新用户资料 |
| PUT | `/password` | 修改密码 |

#### 训练数据 (`/api/v1/training`)
| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/records` | 获取训练记录列表 |
| POST | `/records` | 创建训练记录 |
| PUT | `/records/{id}` | 更新训练记录 |
| DELETE | `/records/{id}` | 删除训练记录 |

#### 负荷与恢复 (`/api/v1/load-recovery`)
| 方法 | 端点 | 说明 |
|------|------|------|
| POST | `/training-data` | 保存训练数据并计算负荷 |
| POST | `/recovery-assessment` | 评估恢复状态 |
| GET | `/training-suggestions` | 获取训练建议 |
| GET | `/load-trend` | 获取负荷趋势 |
| GET | `/one-rep-max` | 计算1RM |

#### 营养管理 (`/api/v1/nutrition`)
| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/records` | 获取营养记录 |
| POST | `/records` | 创建营养记录 |
| GET | `/goals` | 获取营养目标 |
| PUT | `/goals` | 更新营养目标 |

#### 系统监控 (`/api/v1/admin/monitor`) - 需管理员权限
| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/system-info` | 获取系统信息 |
| GET | `/jvm-metrics` | 获取JVM指标 |
| GET | `/database-stats` | 获取数据库统计 |
| GET | `/user-activity` | 获取用户活跃度 |

#### 缓存管理 (`/api/v1/admin/cache`) - 需管理员权限
| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/stats` | 获取缓存统计 |
| POST | `/evict/{cacheName}` | 清除指定缓存 |
| POST | `/evict-all` | 清除所有缓存 |

#### 数据导出 (`/api/v1/admin/export`) - 需管理员权限
| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/users` | 导出用户数据 |
| GET | `/training-records` | 导出训练记录 |
| GET | `/nutrition-records` | 导出营养记录 |

#### 模块配置 (`/api/v1/modules`)
| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/` | 获取所有模块配置 |
| GET | `/enabled` | 获取已启用的模块列表 |

#### 恢复评估 (`/api/v1/recovery`)
| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/status` | 获取当前恢复状态 |
| GET | `/status/{date}` | 获取指定日期的恢复状态 |
| GET | `/suggestions` | 获取训练建议 |
| POST | `/calculate-score` | 计算恢复评分 |

### 认证方式

系统使用 JWT (JSON Web Token) 进行认证：

1. 调用 `/api/v1/auth/login` 获取令牌
2. 在后续请求的 Header 中添加：`Authorization: Bearer <token>`

**示例:**
```bash
# 登录获取令牌
curl -X POST http://localhost:8080/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username": "user", "password": "password"}'

# 使用令牌访问API
curl http://localhost:8080/api/v1/users/profile \
  -H "Authorization: Bearer eyJhbGciOiJIUzUxMiJ9..."
```


## 贡献指南

### 代码规范

#### Java 代码规范

1. **命名规范**
   - 类名：大驼峰命名 (PascalCase)，如 `UserService`
   - 方法名/变量名：小驼峰命名 (camelCase)，如 `getUserById`
   - 常量：全大写下划线分隔，如 `MAX_RETRY_COUNT`
   - 包名：全小写，如 `com.wzl.fitness.service`

2. **注释规范**
   - 类和公共方法必须有 Javadoc 注释
   - 复杂逻辑需要行内注释说明
   - 使用中文或英文注释，保持一致性

3. **代码格式**
   - 使用 4 空格缩进
   - 行宽不超过 120 字符
   - 方法之间空一行
   - 使用 Lombok 简化代码

4. **最佳实践**
   - 使用 `@Slf4j` 进行日志记录
   - 使用 `@Valid` 进行参数验证
   - 异常统一通过 `GlobalExceptionHandler` 处理
   - 敏感操作添加 `@Auditable` 注解

#### Vue/JavaScript 代码规范

1. **命名规范**
   - 组件名：大驼峰命名，如 `UserProfile.vue`
   - 变量/函数：小驼峰命名，如 `getUserData`
   - 常量：全大写下划线分隔，如 `API_BASE_URL`
   - CSS类名：kebab-case，如 `user-profile`

2. **组件规范**
   - 使用 Composition API (`<script setup>`)
   - Props 必须定义类型和默认值
   - 事件名使用 kebab-case

3. **代码格式**
   - 使用 2 空格缩进
   - 使用 ESLint + Prettier 格式化
   - 运行 `npm run lint` 检查代码

### Git 工作流

#### 分支命名

| 分支类型 | 命名格式 | 示例 |
|----------|----------|------|
| 功能分支 | `feature/功能描述` | `feature/user-auth` |
| 修复分支 | `fix/问题描述` | `fix/login-error` |
| 热修复 | `hotfix/问题描述` | `hotfix/security-patch` |
| 发布分支 | `release/版本号` | `release/v1.4.0` |

#### 提交信息规范

使用 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

```
<type>(<scope>): <subject>

<body>

<footer>
```

**类型 (type):**
- `feat`: 新功能
- `fix`: 修复bug
- `docs`: 文档更新
- `style`: 代码格式（不影响功能）
- `refactor`: 重构
- `test`: 测试相关
- `chore`: 构建/工具相关

**示例:**
```
feat(auth): 添加JWT刷新令牌功能

- 实现令牌自动刷新机制
- 添加刷新令牌API端点
- 更新前端拦截器

Closes #123
```

### PR 提交流程

1. **创建分支**
   ```bash
   git checkout -b feature/your-feature
   ```

2. **开发并提交**
   ```bash
   git add .
   git commit -m "feat(scope): your message"
   ```

3. **同步主分支**
   ```bash
   git fetch origin
   git rebase origin/main
   ```

4. **推送分支**
   ```bash
   git push origin feature/your-feature
   ```

5. **创建 Pull Request**
   - 填写清晰的标题和描述
   - 关联相关 Issue
   - 请求代码审查

6. **代码审查**
   - 至少需要 1 位审查者批准
   - 解决所有审查意见
   - 确保 CI 检查通过

7. **合并**
   - 使用 Squash and Merge
   - 删除已合并的分支

### 测试要求

- 新功能必须包含单元测试
- 后端测试覆盖率目标：80%+
- 前端测试覆盖率目标：75%+
- 关键业务逻辑需要属性测试

**运行测试:**
```bash
# 后端测试
cd Fitness
mvn test

# 前端单元测试
cd Fitness/frontend
npm run test:unit

# 前端E2E测试
npm run test:e2e
```

## 数据库配置说明

### H2内存数据库（开发/测试环境）

系统默认使用H2内存数据库，适用于开发和测试：
- 数据库文件：`./fitness_db`
- 控制台访问：`http://localhost:8080/h2-console`
- JDBC URL：`jdbc:h2:file:./fitness_db`
- 用户名：`sa`
- 密码：`password`

**注意**: H2内存数据库在应用重启后会丢失所有数据，如需持久化数据请使用MySQL。

### MySQL数据库（生产环境）

生产环境推荐使用MySQL数据库，配置步骤：
1. 创建数据库：`CREATE DATABASE fitness_db;`
2. 修改 `application.properties` 配置
3. 确保MySQL服务正在运行

## 安全特性

1. **密码加密**: 使用BCrypt算法加密存储密码
2. **JWT认证**: 无状态令牌认证，支持刷新令牌
3. **输入验证**: 使用Jakarta Validation进行数据验证
4. **CORS配置**: 支持跨域请求
5. **异常处理**: 统一的异常处理机制
6. **SQL注入防护**: 使用JPA防止SQL注入
7. **API限流**: 防止恶意请求
8. **登录保护**: 失败锁定机制
9. **审计日志**: 敏感操作记录

## 故障排除指南

### 常见问题及解决方案

#### 1. 登录功能返回500错误
**问题描述**: 用户登录时返回HTTP 500内部服务器错误

**可能原因**: JWT密钥长度不足

**解决方案**:
- 检查 `application.properties` 中的 `jwt.secret` 配置
- 确保密钥长度至少512位（HS512算法要求）

#### 2. 数据库连接失败
**问题描述**: 应用启动时报数据库连接错误

**解决方案**:
- 确认MySQL服务已启动
- 检查数据库连接配置
- 确认数据库用户权限

#### 3. 前端无法连接后端
**问题描述**: 前端请求返回网络错误

**解决方案**:
- 确认后端服务已启动
- 检查CORS配置
- 确认代理配置正确

更多故障排除信息请参考 [部署文档](docs/DEPLOYMENT.md)。

## 更新日志

### v1.5.0 (2026-01-09)
#### 🏗️ 模块化架构重构
- 后端模块化架构实现（用户、训练、营养、恢复、管理模块）
- 前端模块化架构实现（认证、仪表盘、训练、营养、设置模块）
- 模块接口定义和依赖管理
- 事件驱动的模块间通信
- 模块启用/禁用配置支持
- 模块化API分组（Swagger UI）
- OpenAPI文档更新，支持按模块查看API

#### 📝 文档更新
- README添加模块化架构说明
- 更新API文档，添加模块配置接口
- 添加模块配置示例

### v1.4.0 (2026-01-07)
#### 📝 文档完善
- 更新README项目结构说明
- 添加完整的开发环境搭建指南
- 添加API接口概览和Swagger说明
- 更新技术栈说明（含版本信息）
- 添加贡献指南和代码规范

#### 🔧 系统优化
- Caffeine本地缓存实现
- 系统监控API
- 数据导出功能
- 管理端功能增强

### v1.3.0 (2025-12-31)
#### 🧪 测试体系完善
- 前端单元测试框架搭建（Vitest + @vue/test-utils）
- 213个前端单元测试用例，覆盖率65%+
- E2E端到端测试实现（Playwright）
- 后端性能测试（Gatling）- 100并发<500ms响应

#### ⚡ 性能优化
- 路由懒加载实现
- API请求缓存策略
- Lighthouse性能分数达到90分

#### 📱 PWA离线支持
- Service Worker配置
- IndexedDB离线数据存储
- 离线数据同步机制

### v1.2.0 (2025-12-30)
#### 🧪 测试覆盖率提升
- 新增36个属性测试用例（jqwik框架）
- 完善Controller层集成测试

#### 🔒 安全性增强
- API限流机制
- 登录失败锁定
- 审计日志记录

### v1.1.0 (2025-12-24)
#### 🐛 Bug修复
- 修复JWT密钥长度不足导致的登录失败问题

### v1.0.0 (2025-12-23)
#### ✨ 初始版本
- 完整的用户认证系统
- 训练数据管理
- 负荷计算和恢复评估
- 数据可视化

## 系统状态

### ✅ 已验证功能
- [x] 用户注册/登录功能（JWT认证）
- [x] 密码加密存储（BCrypt）
- [x] 训练数据管理
- [x] 负荷计算和恢复评估
- [x] 营养追踪
- [x] 数据可视化
- [x] API限流和登录保护
- [x] 审计日志
- [x] Caffeine缓存
- [x] 系统监控
- [x] 数据导出
- [x] Swagger/OpenAPI文档
- [x] 健康检查端点（Actuator）
- [x] 模块化架构（后端）
- [x] 模块化架构（前端）
- [x] 模块启用/禁用配置
- [x] 模块化API分组
- [x] 事件驱动模块通信

## 许可证

本项目采用 MIT 许可证

## 注意事项

1. 项目构建时推荐使用 `-DskipTests` 参数以跳过测试阶段
2. 生产环境请使用MySQL数据库
3. 确保JWT密钥长度满足安全要求
